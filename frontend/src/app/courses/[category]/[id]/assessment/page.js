import AssessmentInstructions from '@/features/courses/components/AssessmentInstructions';

export default function AssessmentInstructionsRoutePage({ params }) {
  // You can use params.category and params.id if needed for logic
  return <AssessmentInstructions />;
}
